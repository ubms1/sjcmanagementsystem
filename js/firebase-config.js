/* SJC UBMS — localStorage-only mode (GitHub Pages deployment)
   Firebase is disabled. All data is stored on-device via localStorage.
   To enable cloud sync later, configure Firebase in this file. */
window.FIREBASE_ENABLED = false;
window.FIRESTORE         = null;
